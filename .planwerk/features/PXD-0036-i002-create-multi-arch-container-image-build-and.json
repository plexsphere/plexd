{
  "feature_id": "PXD-0036",
  "title": "I002: Create multi-arch container image build and push workflow",
  "slug": "i002-create-multi-arch-container-image-build-and",
  "status": "proposed",
  "phase": null,
  "summary": "",
  "description": "**Size:** 📦 medium\n**Category:** infrastructure\n**Priority:** high\n**Source:** Proposed for 'There are currently no GitHub Actions for the bui'\n\nCreate `.github/workflows/container.yml` that builds and pushes a multi-arch Docker image to `ghcr.io/plexsphere/plexd` on tag push (`v*`) and main branch push.\n\nWorkflow steps:\n1. Checkout code\n2. Set up Docker Buildx via `docker/setup-buildx-action`\n3. Login to ghcr.io via `docker/login-action` using `GITHUB_TOKEN`\n4. Extract image tags via `docker/metadata-action`:\n   - On tag push `v1.2.3`: tags `1.2.3`, `1.2`, `1`, `latest`\n   - On main push: tag `dev`\n5. Build multi-platform image (`linux/amd64,linux/arm64`) using `docker/build-push-action` with:\n   - `context: .`\n   - `file: deploy/docker/Dockerfile` (existing Dockerfile, already supports TARGETOS/TARGETARCH)\n   - Build args for version metadata (VERSION, COMMIT, DATE) matching Dockerfile ARG patterns\n   - `push: true`\n   - Platform: `linux/amd64,linux/arm64`\n\nWorkflow permissions: `packages: write`, `contents: read`.\n\nReference existing files (read-only): `deploy/docker/Dockerfile` (already multi-stage, multi-arch ready), `deploy/kubernetes/daemonset.yaml` (references `ghcr.io/plexsphere/plexd`).\n\n**Rationale:** The Kubernetes DaemonSet manifest references `ghcr.io/plexsphere/plexd` but no workflow pushes images there. Deploying to Kubernetes is impossible without manually building and pushing the container image. This blocks the entire container distribution channel and Kubernetes deployment path.\n\n**Affected Areas:**\n- .github/workflows/container.yml",
  "stories": [],
  "requirements": [],
  "tasks": [],
  "test_specifications": [],
  "affected_files": [],
  "similar_patterns": [],
  "review_criteria": [],
  "implementation_notes": "",
  "status_history": {
    "proposed": {
      "github_account": "berendt",
      "timestamp": "2026-02-15T20:34:37.450559"
    }
  },
  "elaboration": "\n\n**Size:** 🔨 medium\n**Category:** infrastructure\n**Priority:** high\n\n## Summary\n\nThe project has CI (lint, unit tests, integration tests) but no workflows for producing release artifacts. The Dockerfile, Makefile, and `install.sh` all reference build outputs (`plexd-linux-{arch}`, `checksums.sha256`, `ghcr.io/plexsphere/plexd`) that nothing in CI actually generates. Two new GitHub Actions workflows are needed: one for releasing cross-compiled binaries with checksums on tag push (plus `latest`/`dev` on main), and one for building + pushing the multi-arch container image to ghcr.io on the same triggers.\n\n## Scope\n\n**Included:**\n- **`.github/workflows/release.yml`** (new) — Cross-compile `plexd` for linux/amd64 + linux/arm64, generate `checksums.sha256`, create GitHub Release with auto-generated notes on tag push (`v*`)\n- **`.github/workflows/container.yml`** (new) — Build + push multi-arch Docker image to `ghcr.io/plexsphere/plexd` on tag push (`v*`) and main branch pushes, using existing `deploy/docker/Dockerfile`\n- Version metadata injection via ldflags (`VERSION`, `COMMIT`, `DATE`) matching existing Dockerfile/Makefile patterns\n- Image tagging: semver tags (`v1.2.3` → `1.2.3`, `1.2`, `1`, `latest`) on tag push; `dev` tag on main push\n- Binary release triggered on tag push only; container build triggered on tag push + main push\n\n**Excluded (YAGNI):**\n- macOS/Windows binaries — `install.sh` is Linux-only, no current demand\n- Helm chart publishing — no Helm chart exists\n- Signing/SBOM/Cosign — valuable but separate concern, not referenced in codebase\n- Auto-updating `install.sh` artifact URL — script already uses configurable `PLEXD_ARTIFACT_URL`\n- PR-triggered container builds — CI already validates PRs; not worth the cost\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph Triggers\n        TAG[\"Git tag push v*\"]\n        MAIN[\"Push to main\"]\n    end\n\n    subgraph release[\"release.yml — Binary Release\"]\n        MATRIX[\"Build matrix: linux/amd64, linux/arm64\"]\n        BUILD[\"go build with ldflags\"]\n        SHA[\"Generate checksums.sha256\"]\n        GHR[\"Create GitHub Release + upload assets\"]\n        MATRIX --> BUILD --> SHA --> GHR\n    end\n\n    subgraph container[\"container.yml — Container Image\"]\n        DKBX[\"docker buildx multi-platform\"]\n        PUSH[\"Push to ghcr.io/plexsphere/plexd\"]\n        DTAG[\"Tags: semver + latest / dev\"]\n        DKBX --> PUSH --> DTAG\n    end\n\n    TAG --> release\n    TAG --> container\n    MAIN --> container\n\n    subgraph Consumers\n        INST[\"install.sh downloads binary + checksums\"]\n        K8S[\"Kubernetes DaemonSet pulls image\"]\n    end\n\n    GHR -.-> INST\n    DTAG -.-> K8S\n```\n\n```mermaid\nsequenceDiagram\n    participant D as Developer\n    participant GH as GitHub\n    participant WF as Actions Workflow\n    participant REG as ghcr.io\n\n    D->>GH: git tag v1.0.0 + push\n    GH->>WF: trigger release.yml\n    GH->>WF: trigger container.yml\n    par Binary Release\n        WF->>WF: go build linux/amd64\n        WF->>WF: go build linux/arm64\n        WF->>WF: sha256sum checksums.sha256\n        WF->>GH: gh release create --generate-notes\n    and Container Image\n        WF->>WF: docker buildx build --platform linux/amd64,linux/arm64\n        WF->>REG: push ghcr.io/plexsphere/plexd:1.0.0\n        WF->>REG: push ghcr.io/plexsphere/plexd:latest\n    end\n```\n\n## Key Components\n\n- **`.github/workflows/release.yml`** (new): Cross-compile binary for linux/{amd64,arm64}, generate checksums, create GitHub Release with auto-generated notes — artifact names match `install.sh` convention (`plexd-linux-amd64`, `plexd-linux-arm64`, `checksums.sha256`)\n- **`.github/workflows/container.yml`** (new): Build multi-arch image using `deploy/docker/Dockerfile` via `docker/build-push-action`, push to `ghcr.io/plexsphere/plexd` — on tags: semver tags + `latest`; on main: `dev` tag — image reference matches `deploy/kubernetes/daemonset.yaml`\n- **Existing `deploy/docker/Dockerfile`**: Already supports `TARGETOS`/`TARGETARCH` build args and ldflags — no changes needed\n- **Existing `deploy/install.sh`**: Already expects `plexd-linux-{arch}` and `checksums.sha256` at artifact URL — no changes needed\n- **Existing `.github/workflows/ci.yml`**: Remains unchanged; lint/test continues on push+PR as before"
}