{
  "feature_id": "PXD-0039",
  "title": "J003: Implement Kubernetes E2E test with kind cluster and DaemonSet validation",
  "slug": "j003-implement-kubernetes-e2e-test-with-kind",
  "status": "proposed",
  "phase": null,
  "summary": "",
  "description": "**Size:** 🏗️ large\n**Category:** testing\n**Priority:** high\n**Source:** Proposed for 'After binary builds, Docker image builds, etc. are now in place'\n\nCreate Kubernetes E2E test that validates all deploy/kubernetes/ manifests work on a real cluster. Build test/e2e/kubernetes/mock-api-manifests.yaml (Deployment + ClusterIP Service for mock-api). Create test/e2e/kubernetes/test.sh orchestration script that: creates a kind cluster, builds and loads plexd + mock-api images into kind, applies all deploy/kubernetes/ manifests (namespace, RBAC, CRDs, serviceaccount, secret, configmap, DaemonSet), applies mock-api manifests, waits for DaemonSet rollout, port-forwards to mock-api, validates /test/assertions counters, then deletes the kind cluster on cleanup. Add Makefile target test-e2e-k8s.\n\n**Rationale:** The Kubernetes DaemonSet deployment is the primary production deployment mode. Without E2E testing, RBAC misconfigurations, missing CRDs, incorrect serviceaccount references, or DaemonSet scheduling issues would only be caught in production. This validates the entire manifest stack end-to-end.\n\n**Affected Areas:**\n- test/e2e/kubernetes/mock-api-manifests.yaml\n- test/e2e/kubernetes/test.sh\n- deploy/kubernetes/\n- Makefile",
  "stories": [],
  "requirements": [],
  "tasks": [],
  "test_specifications": [],
  "affected_files": [],
  "similar_patterns": [],
  "review_criteria": [],
  "implementation_notes": "",
  "status_history": {
    "proposed": {
      "github_account": "berendt",
      "timestamp": "2026-02-15T22:00:43.566550"
    }
  },
  "elaboration": "\n\n**Size:** 🏗️ large\n**Category:** infrastructure\n**Priority:** high\n\n## Open Questions\n\nNo remaining open questions — the user has resolved all ambiguities:\n- **Mock API scope:** Read-only stubs returning fixture data\n- **Trigger strategy:** Path-filtered triggers\n\n## Summary\n\nAdd end-to-end test workflows in GitHub Actions that validate plexd's Docker image, Kubernetes DaemonSet deployment, and systemd service installation against a fully simulated Plexsphere Central API. A lightweight Go mock server implementing the critical API endpoints (`/v1/register`, `/v1/nodes/{id}/heartbeat`, `/v1/nodes/{id}/state`, `/v1/nodes/{id}/metadata`, SSE `/v1/nodes/{id}/events`) runs as the test backend, allowing plexd to execute its real registration, heartbeat, metadata fetch, and reconciliation flows in each deployment mode.\n\n## Scope\n\n**Included:**\n- Go mock server binary (`net/http`, no frameworks) implementing: `/v1/ping`, `/v1/register`, `/v1/nodes/{id}/heartbeat`, `/v1/nodes/{id}/state`, `/v1/nodes/{id}/metadata`, `/v1/nodes/{id}/events` (SSE), plus `/test/assertions` for programmatic verification — all read-only stubs returning fixture data with in-memory call counters\n- **Docker E2E job** — build image, start mock API + plexd container via compose, verify registration + heartbeat + metadata fetch\n- **Kubernetes E2E job** — `kind` cluster, apply all `deploy/kubernetes/` manifests (namespace, RBAC, CRDs, serviceaccount, secret, configmap, DaemonSet), verify DaemonSet pod readiness + plexd registers and heartbeats against mock API\n- **systemd E2E job** — systemd-in-Docker container, install plexd binary + `deploy/systemd/plexd.service` unit file, start service, verify registration + heartbeat + metadata fetch\n- Makefile targets: `test-e2e-docker`, `test-e2e-k8s`, `test-e2e-systemd` for local execution\n\n**Excluded (YAGNI):**\n- Full 18-endpoint mock — only core lifecycle + metadata endpoints; rest is unit-tested\n- WireGuard mesh validation — requires `NET_ADMIN` + kernel module; infeasible on GHA runners\n- Performance/load testing — separate concern\n- Multi-arch E2E (arm64) — GHA arm64 runners are slow/limited; amd64 validates deployment patterns\n- Cloud-init E2E — requires VM provisioning; too heavy for CI\n- Helm chart — none exists in the project\n- Docker image push to registry — handled by release workflow\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph GHA[\"GitHub Actions - e2e.yml\"]\n        direction TB\n        trigger[\"Push / PR trigger<br/>path filter: cmd/, internal/, deploy/,<br/>test/e2e/, Dockerfile, workflows\"]\n\n        subgraph docker_e2e[\"Docker E2E Job\"]\n            d1[\"Build plexd image\"]\n            d2[\"Build mock-api image\"]\n            d3[\"docker compose up\"]\n            d4[\"Wait for registration\"]\n            d5[\"Assert heartbeat + metadata\"]\n            d6[\"Tear down\"]\n            d1 --> d3\n            d2 --> d3\n            d3 --> d4 --> d5 --> d6\n        end\n\n        subgraph k8s_e2e[\"Kubernetes E2E Job\"]\n            k1[\"Create kind cluster\"]\n            k2[\"Load plexd + mock-api images\"]\n            k3[\"Apply deploy/kubernetes/ manifests\"]\n            k4[\"Deploy mock-api as Service\"]\n            k5[\"Wait DaemonSet ready\"]\n            k6[\"Verify via /test/assertions\"]\n            k7[\"Delete kind cluster\"]\n            k1 --> k2 --> k3 --> k4 --> k5 --> k6 --> k7\n        end\n\n        subgraph systemd_e2e[\"systemd E2E Job\"]\n            s1[\"Start systemd container\"]\n            s2[\"Start mock-api container\"]\n            s3[\"Install plexd binary + unit file\"]\n            s4[\"systemctl start plexd\"]\n            s5[\"Verify registration + heartbeat + metadata\"]\n            s6[\"systemctl stop plexd\"]\n            s1 --> s3\n            s2 --> s3\n            s3 --> s4 --> s5 --> s6\n        end\n\n        trigger --> docker_e2e\n        trigger --> k8s_e2e\n        trigger --> systemd_e2e\n    end\n```\n\n```mermaid\nsequenceDiagram\n    participant plexd as plexd agent\n    participant mock as Mock Central API\n    participant verify as Test Verifier\n\n    plexd->>mock: GET /v1/ping\n    mock-->>plexd: 200 OK\n    plexd->>mock: POST /v1/register\n    mock-->>plexd: 200 {node_id, mesh_ip, peers}\n    plexd->>mock: POST /v1/nodes/{id}/heartbeat\n    mock-->>plexd: 200 {reconcile: true}\n    plexd->>mock: GET /v1/nodes/{id}/state\n    mock-->>plexd: 200 {peers, policies}\n    plexd->>mock: GET /v1/nodes/{id}/metadata\n    mock-->>plexd: 200 {metadata blob}\n    verify->>mock: GET /test/assertions\n    mock-->>verify: {reg_count, hb_count, meta_count}\n```\n\n```mermaid\nflowchart LR\n    subgraph mock_server[\"Mock API Server - test/e2e/mockapi\"]\n        ping[\"/v1/ping\"]\n        reg[\"/v1/register\"]\n        hb[\"/v1/nodes/{id}/heartbeat\"]\n        st[\"/v1/nodes/{id}/state\"]\n        meta[\"/v1/nodes/{id}/metadata\"]\n        events[\"/v1/nodes/{id}/events SSE\"]\n        assert[\"/test/assertions\"]\n    end\n\n    subgraph counters[\"In-Memory Counters\"]\n        rc[\"Registration count\"]\n        hc[\"Heartbeat count\"]\n        mc[\"Metadata fetch count\"]\n        sc[\"State fetch count\"]\n    end\n\n    reg --> rc\n    hb --> hc\n    meta --> mc\n    st --> sc\n    assert --> counters\n```\n\n## Key Components\n\n- **`test/e2e/mockapi/`** — Go mock server binary (`main.go` + `Dockerfile`). Implements 7 endpoints with read-only fixture responses. Tracks call counts in-memory via atomic counters. Exposes `/test/assertions` returning JSON `{registration_count, heartbeat_count, metadata_fetch_count, state_fetch_count, last_heartbeat_at}`.\n- **`test/e2e/docker-compose.yml`** — Compose file with `mock-api` and `plexd` services on shared bridge network. Mock API starts first (healthcheck on `/v1/ping`), plexd configured via `PLEXD_API_URL` env var.\n- **`test/e2e/docker/`** — Docker E2E test script: starts compose, polls `/test/assertions` with timeout, asserts `registration_count >= 1`, `heartbeat_count >= 1`, `metadata_fetch_count >= 1`. Exits non-zero on failure.\n- **`test/e2e/kubernetes/`** — K8s E2E test script + mock-api Kubernetes manifests (Deployment + ClusterIP Service). Uses `kind create cluster`, loads locally-built images, applies `deploy/kubernetes/` manifests + mock-api manifests, waits for DaemonSet rollout, port-forwards to mock-api, validates assertions.\n- **`test/e2e/systemd/`** — systemd E2E test script + systemd-enabled Dockerfile. Copies plexd binary + `deploy/systemd/plexd.service` into container, configures `PLEXD_API_URL` via environment drop-in, starts service via `systemctl`, validates against mock-api on same Docker network.\n- **`.github/workflows/e2e.yml`** — Single workflow with three parallel jobs (`docker-e2e`, `kubernetes-e2e`, `systemd-e2e`). Path-filtered on `cmd/`, `internal/`, `deploy/`, `test/e2e/`, `Dockerfile`, and workflow files. Runs on `ubuntu-latest`. Uploads logs as artifacts on failure.\n- **`Makefile` additions** — Three targets: `test-e2e-docker`, `test-e2e-k8s`, `test-e2e-systemd`. Each self-contained with build + run + teardown."
}